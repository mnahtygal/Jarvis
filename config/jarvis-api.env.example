# Jarvis API service environment example
#
# Copy the variable name into a protected environment file outside this
# repository and set its value locally. Never commit, print, log, or share the
# real token. Use a unique, high-entropy token dedicated to the handheld.

JARVIS_HANDHELD_TOKEN=
